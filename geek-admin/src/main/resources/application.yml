# 项目相关配置
geek:
  # 名称
  name: Geek
  # 版本
  version: 3.8.5
  # 版权年份
  copyrightYear: 2026
  # 文件路径 示例（ Windows配置D:/geek/uploadPath，Linux配置 /home/geek/uploadPath）
  profile: D:/geek/uploadPath

# 本地文件存储根路径（可选）。不配置时默认使用 运行目录/data/files/master，避免 /data 只读导致上传失败
# storage.local.path: D:/geek/data/files/master
  # 获取ip地址开关
  addressEnabled: false
  # 验证码类型 math 数组计算 char 字符验证
  captchaType: math
  # 指定默认文件上传方法最大文件大小(MB)
  fileMaxSize: 50

# 开发环境配置
server:
  # 服务器的HTTP端口，默认为8080
  port: 8080
  servlet:
    # 应用的访问路径
    context-path: /
    encoding:
      enabled: true
      force: true
      charset: UTF-8
  tomcat:
    # tomcat的URI编码
    uri-encoding: UTF-8
    # 连接数满后的排队数，默认为100
    accept-count: 1000
    threads:
      # tomcat最大线程数，默认为200
      max: 800
      # Tomcat启动初始化的线程数，默认值10
      min-spare: 100

# 日志配置
logging:
  level:
    "[com.geek]": INFO
    "[org]": WARN
    "[org.springframework]": WARN
    "[org.apache]": WARN
    "[org.springframework.context.support.PostProcessorRegistrationDelegate]": ERROR
    "[com.alibaba.druid.spring.boot3.autoconfigure.stat.DruidSpringAopConfiguration]": ERROR
    "[com.alibaba.druid.spring.boot3.autoconfigure.properties.DruidStatProperties]": ERROR
    # 打开 Mapper 层 SQL 日志（依赖 mybatis-config.xml 中 logImpl = SLF4J）
    "com.geek.**.mapper": DEBUG

# 用户配置
user:
  password:
    # 密码最大错误次数
    maxRetryCount: 5
    # 密码锁定时间（默认10分钟）
    lockTime: 10
  ip:
    maxRetryCount: 15
    lockTime: 15

# Spring配置
spring:
  #给项目来个名字
  application:
    name: geek
  cache:
    # 指定缓存类型 simple 默认缓存 jcache 本地缓存 redis 缓存（去plugins配置链接信息）
    type: simple
  jackson:
    time-zone: UTC
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  # 资源信息
  messages:
    # 国际化资源文件路径
    basename: i18n/messages
  profiles:
    active: data,auth,pay,plugins,model,apidoc
  # 文件上传
  servlet:
    multipart:
      # 单个文件大小
      max-file-size: 10MB
      # 设置总上传的文件大小
      max-request-size: 20MB
  # 服务模块（禁止热部署，避免小改动触发重新编译与重启）
  devtools:
    restart:
      enabled: false

# token配置
token:
  # 令牌自定义标识
  header: Authorization
  # 令牌密钥
  secret: abcdefghijklmnopqrstuvwxyzabcdefghijklmnopqrstuvwxyzabcdefghijklmnopqrstuvwxyz
  # 令牌有效期（默认1天）
  expireTime: 1440

# 防盗链配置
referer:
  # 防盗链开关
  enabled: false
  # 允许的域名列表
  allowed-domains: localhost,127.0.0.1,geek.vip,www.geek.vip
  
# 防止XSS攻击
xss:
  # 过滤开关
  enabled: true
  # 排除链接（多个用逗号分隔）
  excludes: /system/notice
  # 匹配链接
  urlPatterns: /system/*,/monitor/*,/tool/*

security:
  # 白名单配置，多个用逗号分隔
  whitList: /login,/register,/captchaImage,/swagger-ui/**,/swagger-resources/**,/webjars/**,/druid/**,/*/api-docs/**,/websocket/**

# PageHelper分页插件
pagehelper:
  helperDialect: postgresql
  supportMethodsArguments: true
  params: count=countSql

# 配置数据源和mybatis-flex
mybatis-flex:
  # 搜索指定包别名
  typeAliasesPackage: com.geek.**.domain
  # 配置mapper的扫描，找到所有的mapper.xml映射文件
  mapperLocations: classpath*:mapper/**/*Mapper.xml
  # 加载全局的配置文件
  configLocation: classpath:mybatis/mybatis-config.xml

aj:
  captcha:
    cache-type: other
    slip-offset: 10
    aes-status: true
    water-mark: "RuoYi-Geek"
